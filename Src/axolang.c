/* A parser generated by PackCC 3.1.0 */

#ifdef _MSC_VER
#undef _CRT_NONSTDC_NO_WARNINGS
#undef _CRT_SECURE_NO_WARNINGS
#define _CRT_NONSTDC_NO_WARNINGS
#define _CRT_SECURE_NO_WARNINGS
#endif /* _MSC_VER */
#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "axolang.h"

#define PCC_VERSION "3.1.0"

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* 0 = LuaJIT, 1 = C23 */
int BACKEND_TARGET = 0;
int MAIN_ABIERTO = 0; // Controla que el main solo se abra una vez

void asegurar_main() {
if (BACKEND_TARGET == 1 && !MAIN_ABIERTO) {
printf("int main() {\n");
MAIN_ABIERTO = 1;
}
}

#if !defined __has_attribute || defined _MSC_VER
#define __attribute__(x)
#endif

#ifdef _MSC_VER
#define MARK_FUNC_AS_USED __pragma(warning(suppress:4505))
#else
#define MARK_FUNC_AS_USED __attribute__((__unused__))
#endif

#ifdef _MSC_VER
#define MARK_VAR_AS_USED __pragma(warning(suppress:4189))
#else
#define MARK_VAR_AS_USED __attribute__((__unused__))
#endif

#ifndef PCC_BUFFER_MIN_SIZE
#define PCC_BUFFER_MIN_SIZE 256
#endif /* !PCC_BUFFER_MIN_SIZE */

#ifndef PCC_ARRAY_MIN_SIZE
#define PCC_ARRAY_MIN_SIZE 2
#endif /* !PCC_ARRAY_MIN_SIZE */

#ifndef PCC_THUNK_ARRAY_MIN_SIZE
#define PCC_THUNK_ARRAY_MIN_SIZE 2
#endif /* !PCC_THUNK_ARRAY_MIN_SIZE */

#ifndef PCC_LR_MEMO_MAP_MIN_SIZE
#define PCC_LR_MEMO_MAP_MIN_SIZE 4
#endif /* !PCC_LR_MEMO_MAP_MIN_SIZE */

#ifndef PCC_POOL_MIN_SIZE
#define PCC_POOL_MIN_SIZE 65536
#endif /* !PCC_POOL_MIN_SIZE */

#define PCC_DBG_EVALUATE 0
#define PCC_DBG_MATCH    1
#define PCC_DBG_NOMATCH  2

#define PCC_VOID_VALUE (~(size_t)0)

typedef enum pcc_bool_tag {
    PCC_FALSE = 0,
    PCC_TRUE
} pcc_bool_t;

typedef struct pcc_char_array_tag {
    size_t m, n;
    char *p;
} pcc_char_array_t;

typedef struct pcc_range_tag {
    size_t start;
    size_t end;
} pcc_range_t;

typedef char *pcc_value_t;

typedef void *pcc_auxil_t;

typedef axolang_context_t pcc_context_t;

typedef struct pcc_value_table_tag {
    size_t m, n;
    pcc_value_t *p;
} pcc_value_table_t;

typedef struct pcc_value_refer_table_tag {
    size_t m, n;
    pcc_value_t **p;
} pcc_value_refer_table_t;

typedef struct pcc_capture_tag {
    pcc_range_t range;
    pcc_char_array_t string; /* mutable */
} pcc_capture_t;

typedef struct pcc_capture_table_tag {
    size_t m, n;
    pcc_capture_t *p;
} pcc_capture_table_t;

typedef struct pcc_capture_const_table_tag {
    size_t m, n;
    const pcc_capture_t **p;
} pcc_capture_const_table_t;

typedef struct pcc_thunk_tag pcc_thunk_t;
typedef struct pcc_thunk_array_tag pcc_thunk_array_t;

typedef void (*pcc_action_t)(pcc_context_t *, pcc_thunk_t *, pcc_value_t *);

typedef enum pcc_thunk_type_tag {
    PCC_THUNK_LEAF,
    PCC_THUNK_NODE
} pcc_thunk_type_t;

typedef struct pcc_thunk_leaf_tag {
    pcc_value_refer_table_t values;
    pcc_capture_const_table_t capts;
    pcc_capture_t capt0;
    pcc_action_t action;
} pcc_thunk_leaf_t;

typedef struct pcc_thunk_node_tag {
    const pcc_thunk_array_t *thunks; /* just a reference */
    pcc_value_t *value; /* just a reference */
} pcc_thunk_node_t;

typedef union pcc_thunk_data_tag {
    pcc_thunk_leaf_t leaf;
    pcc_thunk_node_t node;
} pcc_thunk_data_t;

struct pcc_thunk_tag {
    pcc_thunk_type_t type;
    pcc_thunk_data_t data;
};

struct pcc_thunk_array_tag {
    size_t m, n;
    pcc_thunk_t **p;
    pcc_thunk_t *a[PCC_THUNK_ARRAY_MIN_SIZE]; /* for reducing memory allocation frequency */
};

typedef struct pcc_thunk_chunk_tag {
    pcc_value_table_t values;
    pcc_capture_table_t capts;
    pcc_thunk_array_t thunks;
    size_t pos; /* the starting position in the character buffer */
} pcc_thunk_chunk_t;

typedef struct pcc_lr_entry_tag pcc_lr_entry_t;

typedef enum pcc_lr_answer_type_tag {
    PCC_LR_ANSWER_LR,
    PCC_LR_ANSWER_CHUNK
} pcc_lr_answer_type_t;

typedef union pcc_lr_answer_data_tag {
    pcc_lr_entry_t *lr;
    pcc_thunk_chunk_t *chunk;
} pcc_lr_answer_data_t;

typedef struct pcc_lr_answer_tag pcc_lr_answer_t;

struct pcc_lr_answer_tag {
    pcc_lr_answer_type_t type;
    pcc_lr_answer_data_t data;
    size_t pos; /* the absolute position in the input */
    pcc_lr_answer_t *hold;
};

typedef pcc_thunk_chunk_t *(*pcc_rule_t)(pcc_context_t *);

typedef struct pcc_rule_set_tag {
    size_t m, n;
    pcc_rule_t *p;
} pcc_rule_set_t;

typedef struct pcc_lr_head_tag pcc_lr_head_t;

struct pcc_lr_head_tag {
    pcc_rule_t rule;
    pcc_rule_set_t invol;
    pcc_rule_set_t eval;
    pcc_lr_head_t *hold;
};

typedef struct pcc_lr_memo_tag {
    pcc_rule_t rule;
    pcc_lr_answer_t *answer;
} pcc_lr_memo_t;

typedef struct pcc_lr_memo_map_tag {
    size_t m, n;
    pcc_lr_memo_t *p;
    pcc_lr_memo_t a[PCC_LR_MEMO_MAP_MIN_SIZE]; /* for reducing memory allocation frequency */
} pcc_lr_memo_map_t;

typedef struct pcc_lr_table_entry_tag {
    pcc_lr_head_t *head; /* just a reference */
    pcc_lr_memo_map_t memos;
    pcc_lr_answer_t *hold_a;
    pcc_lr_head_t *hold_h;
} pcc_lr_table_entry_t;

typedef struct pcc_lr_table_tag {
    size_t m, n;
    size_t o;
    pcc_lr_table_entry_t **p;
} pcc_lr_table_t;

struct pcc_lr_entry_tag {
    pcc_rule_t rule;
    pcc_thunk_chunk_t *seed; /* just a reference */
    pcc_lr_head_t *head; /* just a reference */
};

typedef struct pcc_lr_stack_tag {
    size_t m, n;
    pcc_lr_entry_t **p;
} pcc_lr_stack_t;

typedef struct pcc_memory_entry_tag pcc_memory_entry_t;
typedef struct pcc_memory_pool_tag pcc_memory_pool_t;

struct pcc_memory_entry_tag {
    pcc_memory_entry_t *next;
};

struct pcc_memory_pool_tag {
    pcc_memory_pool_t *next;
    size_t allocated;
    size_t unused;
};

typedef struct pcc_memory_recycler_tag {
    pcc_memory_pool_t *pool_list;
    pcc_memory_entry_t *entry_list;
    size_t element_size;
} pcc_memory_recycler_t;

struct axolang_context_tag {
    size_t pos; /* the position in the input of the first character currently buffered */
    size_t cur; /* the current parsing position in the character buffer */
    size_t level;
    pcc_char_array_t buffer;
    pcc_lr_table_t lrtable;
    pcc_lr_stack_t lrstack;
    pcc_thunk_array_t thunks;
    pcc_capture_t capt0; /* used only for programmable predicates */
    pcc_auxil_t auxil;
    pcc_memory_recycler_t thunk_recycler;
    pcc_memory_recycler_t thunk_chunk_recycler;
    pcc_memory_recycler_t lr_head_recycler;
    pcc_memory_recycler_t lr_answer_recycler;
    pcc_memory_recycler_t lr_table_entry_recycler;
    pcc_memory_recycler_t lr_entry_recycler;
};

#ifndef PCC_ERROR
#define PCC_ERROR(auxil) pcc_error()
MARK_FUNC_AS_USED
static void pcc_error(void) {
    fprintf(stderr, "Syntax error\n");
    exit(1);
}
#endif /* !PCC_ERROR */

#ifndef PCC_GETCHAR
#define PCC_GETCHAR(auxil) getchar()
#endif /* !PCC_GETCHAR */

#ifndef PCC_MALLOC
#define PCC_MALLOC(auxil, size) pcc_malloc_e(size)
static void *pcc_malloc_e(size_t size) {
    void *const p = malloc(size);
    if (p == NULL) {
        fprintf(stderr, "Out of memory\n");
        exit(1);
    }
    return p;
}
#endif /* !PCC_MALLOC */

#ifndef PCC_REALLOC
#define PCC_REALLOC(auxil, ptr, size) pcc_realloc_e(ptr, size)
static void *pcc_realloc_e(void *ptr, size_t size) {
    void *const p = realloc(ptr, size);
    if (p == NULL) {
        fprintf(stderr, "Out of memory\n");
        exit(1);
    }
    return p;
}
#endif /* !PCC_REALLOC */

#ifndef PCC_FREE
#define PCC_FREE(auxil, ptr) free(ptr)
#endif /* !PCC_FREE */

#ifndef PCC_DEBUG
#define PCC_DEBUG(auxil, event, rule, level, pos, buffer, length) ((void)0)
#endif /* !PCC_DEBUG */

static void pcc_char_array__initialize(pcc_auxil_t auxil, pcc_char_array_t *obj) {
    obj->m = 0;
    obj->n = 0;
    obj->p = NULL;
}

static void pcc_char_array__finalize(pcc_auxil_t auxil, pcc_char_array_t *obj) {
    PCC_FREE(auxil, obj->p);
}

static void pcc_char_array__resize(pcc_auxil_t auxil, pcc_char_array_t *obj, size_t len) {
    if (obj->m < len) {
        size_t m = obj->m;
        if (m == 0) m = PCC_BUFFER_MIN_SIZE;
        while (m < len && m != 0) m <<= 1;
        if (m == 0) m = len;
        obj->p = (char *)PCC_REALLOC(auxil, obj->p, m);
        obj->m = m;
    }
    obj->n = len;
}

static void pcc_char_array__add(pcc_auxil_t auxil, pcc_char_array_t *obj, char ch) {
    if (obj->m <= obj->n) {
        const size_t n = obj->n + 1;
        size_t m = obj->m;
        if (m == 0) m = PCC_BUFFER_MIN_SIZE;
        while (m < n && m != 0) m <<= 1;
        if (m == 0) m = n;
        obj->p = (char *)PCC_REALLOC(auxil, obj->p, m);
        obj->m = m;
    }
    obj->p[obj->n++] = ch;
}

static void pcc_value_table__initialize(pcc_auxil_t auxil, pcc_value_table_t *obj) {
    obj->m = 0;
    obj->n = 0;
    obj->p = NULL;
}

static void pcc_value_table__finalize(pcc_auxil_t auxil, pcc_value_table_t *obj) {
    PCC_FREE(auxil, obj->p);
}

static void pcc_value_refer_table__initialize(pcc_auxil_t auxil, pcc_value_refer_table_t *obj) {
    obj->m = 0;
    obj->n = 0;
    obj->p = NULL;
}

static void pcc_value_refer_table__finalize(pcc_auxil_t auxil, pcc_value_refer_table_t *obj) {
    PCC_FREE(auxil, obj->p);
}

static void pcc_value_refer_table__resize(pcc_auxil_t auxil, pcc_value_refer_table_t *obj, size_t len) {
    size_t i;
    if (obj->m < len) {
        size_t m = obj->m;
        if (m == 0) m = PCC_ARRAY_MIN_SIZE;
        while (m < len && m != 0) m <<= 1;
        if (m == 0) m = len;
        obj->p = (pcc_value_t **)PCC_REALLOC(auxil, obj->p, sizeof(pcc_value_t *) * m);
        obj->m = m;
    }
    for (i = obj->n; i < len; i++) obj->p[i] = NULL;
    obj->n = len;
}

static void pcc_capture__initialize(pcc_auxil_t auxil, pcc_capture_t *obj) {
    obj->range.start = 0;
    obj->range.end = 0;
    pcc_char_array__initialize(auxil, &(obj->string));
}

static void pcc_capture__finalize(pcc_auxil_t auxil, pcc_capture_t *obj) {
    pcc_char_array__finalize(auxil, &(obj->string));
}

static void pcc_capture_table__initialize(pcc_auxil_t auxil, pcc_capture_table_t *obj) {
    obj->m = 0;
    obj->n = 0;
    obj->p = NULL;
}

static void pcc_capture_table__finalize(pcc_auxil_t auxil, pcc_capture_table_t *obj) {
    while (obj->n > 0) {
        obj->n--;
        pcc_capture__finalize(auxil, &(obj->p[obj->n]));
    }
    PCC_FREE(auxil, obj->p);
}

static void pcc_capture_table__resize(pcc_auxil_t auxil, pcc_capture_table_t *obj, size_t len) {
    size_t i;
    for (i = len; i < obj->n; i++) pcc_capture__finalize(auxil, &(obj->p[i]));
    if (obj->m < len) {
        size_t m = obj->m;
        if (m == 0) m = PCC_ARRAY_MIN_SIZE;
        while (m < len && m != 0) m <<= 1;
        if (m == 0) m = len;
        obj->p = (pcc_capture_t *)PCC_REALLOC(auxil, obj->p, sizeof(pcc_capture_t) * m);
        obj->m = m;
    }
    for (i = obj->n; i < len; i++) pcc_capture__initialize(auxil, &(obj->p[i]));
    obj->n = len;
}

static void pcc_capture_const_table__initialize(pcc_auxil_t auxil, pcc_capture_const_table_t *obj) {
    obj->m = 0;
    obj->n = 0;
    obj->p = NULL;
}

static void pcc_capture_const_table__finalize(pcc_auxil_t auxil, pcc_capture_const_table_t *obj) {
    PCC_FREE(auxil, (void *)obj->p);
}

static void pcc_capture_const_table__resize(pcc_auxil_t auxil, pcc_capture_const_table_t *obj, size_t len) {
    size_t i;
    if (obj->m < len) {
        size_t m = obj->m;
        if (m == 0) m = PCC_ARRAY_MIN_SIZE;
        while (m < len && m != 0) m <<= 1;
        if (m == 0) m = len;
        obj->p = (const pcc_capture_t **)PCC_REALLOC(auxil, (pcc_capture_t **)obj->p, sizeof(const pcc_capture_t *) * m);
        obj->m = m;
    }
    for (i = obj->n; i < len; i++) obj->p[i] = NULL;
    obj->n = len;
}

static void pcc_memory_recycler__initialize(pcc_auxil_t auxil, pcc_memory_recycler_t *obj, size_t element_size) {
    obj->pool_list = NULL;
    obj->entry_list = NULL;
    obj->element_size = element_size;
}

static void pcc_memory_recycler__finalize(pcc_auxil_t auxil, pcc_memory_recycler_t *obj) {
    while (obj->pool_list) {
        pcc_memory_pool_t *const pool = obj->pool_list;
        obj->pool_list = pool->next;
        PCC_FREE(auxil, pool);
    }
}

static void *pcc_memory_recycler__supply(pcc_auxil_t auxil, pcc_memory_recycler_t *obj) {
    if (obj->entry_list) {
        pcc_memory_entry_t *const tmp = obj->entry_list;
        obj->entry_list = tmp->next;
        return tmp;
    }
    if (!obj->pool_list || obj->pool_list->unused == 0) {
        size_t size = PCC_POOL_MIN_SIZE;
        if (obj->pool_list) {
            size = obj->pool_list->allocated << 1;
            if (size == 0) size = obj->pool_list->allocated;
        }
        {
            pcc_memory_pool_t *const pool = (pcc_memory_pool_t *)PCC_MALLOC(
                auxil, sizeof(pcc_memory_pool_t) + obj->element_size * size
            );
            pool->allocated = size;
            pool->unused = size;
            pool->next = obj->pool_list;
            obj->pool_list = pool;
        }
    }
    obj->pool_list->unused--;
    return (char *)(obj->pool_list + 1) + obj->element_size * obj->pool_list->unused;
}

static void pcc_memory_recycler__recycle(pcc_auxil_t auxil, pcc_memory_recycler_t *obj, void *ptr) {
    pcc_memory_entry_t *const tmp = (pcc_memory_entry_t *)ptr;
    if (tmp == NULL) return;
    tmp->next = obj->entry_list;
    obj->entry_list = tmp;
}

static pcc_thunk_t *pcc_thunk__create_leaf(pcc_context_t *ctx, pcc_action_t action, size_t valuec, size_t captc) {
    pcc_thunk_t *const obj = (pcc_thunk_t *)pcc_memory_recycler__supply(ctx->auxil, &(ctx->thunk_recycler));
    obj->type = PCC_THUNK_LEAF;
    pcc_value_refer_table__initialize(ctx->auxil, &(obj->data.leaf.values));
    pcc_value_refer_table__resize(ctx->auxil, &(obj->data.leaf.values), valuec);
    pcc_capture_const_table__initialize(ctx->auxil, &(obj->data.leaf.capts));
    pcc_capture_const_table__resize(ctx->auxil, &(obj->data.leaf.capts), captc);
    pcc_capture__initialize(ctx->auxil, &(obj->data.leaf.capt0));
    obj->data.leaf.action = action;
    return obj;
}

static pcc_thunk_t *pcc_thunk__create_node(pcc_context_t *ctx, const pcc_thunk_array_t *thunks, pcc_value_t *value) {
    pcc_thunk_t *const obj = (pcc_thunk_t *)pcc_memory_recycler__supply(ctx->auxil, &(ctx->thunk_recycler));
    obj->type = PCC_THUNK_NODE;
    obj->data.node.thunks = thunks;
    obj->data.node.value = value;
    return obj;
}

static void pcc_thunk__destroy(pcc_context_t *ctx, pcc_thunk_t *obj) {
    if (obj == NULL) return;
    switch (obj->type) {
    case PCC_THUNK_LEAF:
        pcc_value_refer_table__finalize(ctx->auxil, &(obj->data.leaf.values));
        pcc_capture_const_table__finalize(ctx->auxil, &(obj->data.leaf.capts));
        pcc_capture__finalize(ctx->auxil, &(obj->data.leaf.capt0));
        break;
    case PCC_THUNK_NODE:
        break;
    default: /* unknown */
        break;
    }
    pcc_memory_recycler__recycle(ctx->auxil, &(ctx->thunk_recycler), obj);
}

static void pcc_thunk_array__initialize(pcc_context_t *ctx, pcc_thunk_array_t *obj) {
    obj->m = PCC_THUNK_ARRAY_MIN_SIZE;
    obj->n = 0;
    obj->p = obj->a;
}

static void pcc_thunk_array__finalize(pcc_context_t *ctx, pcc_thunk_array_t *obj) {
    while (obj->n > 0) {
        obj->n--;
        pcc_thunk__destroy(ctx, obj->p[obj->n]);
    }
    if (obj->p != obj->a) PCC_FREE(ctx->auxil, obj->p);
}

static void pcc_thunk_array__add(pcc_context_t *ctx, pcc_thunk_array_t *obj, pcc_thunk_t *thunk) {
    if (obj->m <= obj->n) {
        const size_t n = obj->n + 1;
        size_t m = obj->m;
        if (m == 0) m = PCC_THUNK_ARRAY_MIN_SIZE << 1;
        while (m < n && m != 0) m <<= 1;
        if (m == 0) m = n;
        if (obj->p == obj->a) {
            size_t i;
            obj->p = (pcc_thunk_t **)PCC_MALLOC(ctx->auxil, sizeof(pcc_thunk_t *) * m);
            for (i = 0; i < PCC_THUNK_ARRAY_MIN_SIZE; i++) obj->p[i] = obj->a[i];
        }
        else {
            obj->p = (pcc_thunk_t **)PCC_REALLOC(ctx->auxil, obj->p, sizeof(pcc_thunk_t *) * m);
        }
        obj->m = m;
    }
    obj->p[obj->n++] = thunk;
}

static void pcc_thunk_array__revert(pcc_context_t *ctx, pcc_thunk_array_t *obj, size_t len) {
    while (obj->n > len) {
        obj->n--;
        pcc_thunk__destroy(ctx, obj->p[obj->n]);
    }
}

static pcc_thunk_chunk_t *pcc_thunk_chunk__create(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const obj = (pcc_thunk_chunk_t *)pcc_memory_recycler__supply(ctx->auxil, &(ctx->thunk_chunk_recycler));
    pcc_value_table__initialize(ctx->auxil, &(obj->values));
    pcc_capture_table__initialize(ctx->auxil, &(obj->capts));
    pcc_thunk_array__initialize(ctx, &(obj->thunks));
    obj->pos = 0;
    return obj;
}

static void pcc_thunk_chunk__destroy(pcc_context_t *ctx, pcc_thunk_chunk_t *obj) {
    if (obj == NULL) return;
    pcc_value_table__finalize(ctx->auxil, &(obj->values));
    pcc_capture_table__finalize(ctx->auxil, &(obj->capts));
    pcc_thunk_array__finalize(ctx, &(obj->thunks));
    pcc_memory_recycler__recycle(ctx->auxil, &(ctx->thunk_chunk_recycler), obj);
}

static void pcc_rule_set__initialize(pcc_auxil_t auxil, pcc_rule_set_t *obj) {
    obj->m = 0;
    obj->n = 0;
    obj->p = NULL;
}

static void pcc_rule_set__finalize(pcc_auxil_t auxil, pcc_rule_set_t *obj) {
    PCC_FREE(auxil, obj->p);
}

static void pcc_rule_set__clear(pcc_auxil_t auxil, pcc_rule_set_t *obj) {
    obj->n = 0;
}

static size_t pcc_rule_set__index(pcc_auxil_t auxil, const pcc_rule_set_t *obj, pcc_rule_t rule) {
    size_t i;
    for (i = 0; i < obj->n; i++) {
        if (obj->p[i] == rule) return i;
    }
    return PCC_VOID_VALUE;
}

static pcc_bool_t pcc_rule_set__add(pcc_auxil_t auxil, pcc_rule_set_t *obj, pcc_rule_t rule) {
    const size_t i = pcc_rule_set__index(auxil, obj, rule);
    if (i != PCC_VOID_VALUE) return PCC_FALSE;
    if (obj->m <= obj->n) {
        const size_t n = obj->n + 1;
        size_t m = obj->m;
        if (m == 0) m = PCC_ARRAY_MIN_SIZE;
        while (m < n && m != 0) m <<= 1;
        if (m == 0) m = n;
        obj->p = (pcc_rule_t *)PCC_REALLOC(auxil, obj->p, sizeof(pcc_rule_t) * m);
        obj->m = m;
    }
    obj->p[obj->n++] = rule;
    return PCC_TRUE;
}

static pcc_bool_t pcc_rule_set__remove(pcc_auxil_t auxil, pcc_rule_set_t *obj, pcc_rule_t rule) {
    const size_t i = pcc_rule_set__index(auxil, obj, rule);
    if (i == PCC_VOID_VALUE) return PCC_FALSE;
    memmove(obj->p + i, obj->p + (i + 1), sizeof(pcc_rule_t) * (obj->n - (i + 1)));
    return PCC_TRUE;
}

static void pcc_rule_set__copy(pcc_auxil_t auxil, pcc_rule_set_t *obj, const pcc_rule_set_t *src) {
    size_t i;
    if (obj == src) return;
    pcc_rule_set__clear(auxil, obj);
    for (i = 0; i < src->n; i++) {
        pcc_rule_set__add(auxil, obj, src->p[i]);
    }
}

static pcc_lr_head_t *pcc_lr_head__create(pcc_context_t *ctx, pcc_rule_t rule) {
    pcc_lr_head_t *const obj = (pcc_lr_head_t *)pcc_memory_recycler__supply(ctx->auxil, &(ctx->lr_head_recycler));
    obj->rule = rule;
    pcc_rule_set__initialize(ctx->auxil, &(obj->invol));
    pcc_rule_set__initialize(ctx->auxil, &(obj->eval));
    obj->hold = NULL;
    return obj;
}

static void pcc_lr_head__destroy(pcc_context_t *ctx, pcc_lr_head_t *obj) {
    if (obj == NULL) return;
    pcc_rule_set__finalize(ctx->auxil, &(obj->invol));
    pcc_rule_set__finalize(ctx->auxil, &(obj->eval));
    pcc_lr_head__destroy(ctx, obj->hold);
    pcc_memory_recycler__recycle(ctx->auxil, &(ctx->lr_head_recycler), obj);
}

static void pcc_lr_entry__destroy(pcc_context_t *ctx, pcc_lr_entry_t *lr);

static pcc_lr_answer_t *pcc_lr_answer__create(pcc_context_t *ctx, pcc_lr_answer_type_t type, size_t pos) {
    pcc_lr_answer_t *obj = (pcc_lr_answer_t *)pcc_memory_recycler__supply(ctx->auxil, &(ctx->lr_answer_recycler));
    obj->type = type;
    obj->pos = pos;
    obj->hold = NULL;
    switch (obj->type) {
    case PCC_LR_ANSWER_LR:
        obj->data.lr = NULL;
        break;
    case PCC_LR_ANSWER_CHUNK:
        obj->data.chunk = NULL;
        break;
    default: /* unknown */
        pcc_memory_recycler__recycle(ctx->auxil, &(ctx->lr_answer_recycler), obj);
        obj = NULL;
    }
    return obj;
}

static void pcc_lr_answer__destroy(pcc_context_t *ctx, pcc_lr_answer_t *obj) {
    while (obj != NULL) {
        pcc_lr_answer_t *const a = obj->hold;
        switch (obj->type) {
        case PCC_LR_ANSWER_LR:
            pcc_lr_entry__destroy(ctx, obj->data.lr);
            break;
        case PCC_LR_ANSWER_CHUNK:
            pcc_thunk_chunk__destroy(ctx, obj->data.chunk);
            break;
        default: /* unknown */
            break;
        }
        pcc_memory_recycler__recycle(ctx->auxil, &(ctx->lr_answer_recycler), obj);
        obj = a;
    }
}

static void pcc_lr_answer__set_chunk(pcc_context_t *ctx, pcc_lr_answer_t *obj, pcc_thunk_chunk_t *chunk) {
    pcc_lr_answer_t *const a = pcc_lr_answer__create(ctx, obj->type, obj->pos);
    switch (obj->type) {
    case PCC_LR_ANSWER_LR:
        a->data.lr = obj->data.lr;
        break;
    case PCC_LR_ANSWER_CHUNK:
        a->data.chunk = obj->data.chunk;
        break;
    default: /* unknown */
        break;
    }
    a->hold = obj->hold;
    obj->hold = a;
    obj->type = PCC_LR_ANSWER_CHUNK;
    obj->data.chunk = chunk;
}

static void pcc_lr_memo_map__initialize(pcc_context_t *ctx, pcc_lr_memo_map_t *obj) {
    obj->m = PCC_LR_MEMO_MAP_MIN_SIZE;
    obj->n = 0;
    obj->p = obj->a;
}

static void pcc_lr_memo_map__finalize(pcc_context_t *ctx, pcc_lr_memo_map_t *obj) {
    while (obj->n > 0) {
        obj->n--;
        pcc_lr_answer__destroy(ctx, obj->p[obj->n].answer);
    }
    if (obj->p != obj->a) PCC_FREE(ctx->auxil, obj->p);
}

static size_t pcc_lr_memo_map__index(pcc_context_t *ctx, pcc_lr_memo_map_t *obj, pcc_rule_t rule) {
    size_t i;
    for (i = 0; i < obj->n; i++) {
        if (obj->p[i].rule == rule) return i;
    }
    return PCC_VOID_VALUE;
}

static void pcc_lr_memo_map__put(pcc_context_t *ctx, pcc_lr_memo_map_t *obj, pcc_rule_t rule, pcc_lr_answer_t *answer) {
    const size_t i = pcc_lr_memo_map__index(ctx, obj, rule);
    if (i != PCC_VOID_VALUE) {
        pcc_lr_answer__destroy(ctx, obj->p[i].answer);
        obj->p[i].answer = answer;
    }
    else {
        if (obj->m <= obj->n) {
            const size_t n = obj->n + 1;
            size_t m = obj->m;
            if (m == 0) m = PCC_LR_MEMO_MAP_MIN_SIZE << 1;
            while (m < n && m != 0) m <<= 1;
            if (m == 0) m = n;
            if (obj->p == obj->a) {
                size_t i;
                obj->p = (pcc_lr_memo_t *)PCC_MALLOC(ctx->auxil, sizeof(pcc_lr_memo_t) * m);
                for (i = 0; i < PCC_LR_MEMO_MAP_MIN_SIZE; i++) obj->p[i] = obj->a[i];
            }
            else {
                obj->p = (pcc_lr_memo_t *)PCC_REALLOC(ctx->auxil, obj->p, sizeof(pcc_lr_memo_t) * m);
            }
            obj->m = m;
        }
        obj->p[obj->n].rule = rule;
        obj->p[obj->n].answer = answer;
        obj->n++;
    }
}

static pcc_lr_answer_t *pcc_lr_memo_map__get(pcc_context_t *ctx, pcc_lr_memo_map_t *obj, pcc_rule_t rule) {
    const size_t i = pcc_lr_memo_map__index(ctx, obj, rule);
    return (i != PCC_VOID_VALUE) ? obj->p[i].answer : NULL;
}

static pcc_lr_table_entry_t *pcc_lr_table_entry__create(pcc_context_t *ctx) {
    pcc_lr_table_entry_t *const obj = (pcc_lr_table_entry_t *)pcc_memory_recycler__supply(ctx->auxil, &(ctx->lr_table_entry_recycler));
    obj->head = NULL;
    pcc_lr_memo_map__initialize(ctx, &(obj->memos));
    obj->hold_a = NULL;
    obj->hold_h = NULL;
    return obj;
}

static void pcc_lr_table_entry__destroy(pcc_context_t *ctx, pcc_lr_table_entry_t *obj) {
    if (obj == NULL) return;
    pcc_lr_memo_map__finalize(ctx, &(obj->memos));
    pcc_lr_answer__destroy(ctx, obj->hold_a);
    pcc_lr_head__destroy(ctx, obj->hold_h);
    pcc_memory_recycler__recycle(ctx->auxil, &(ctx->lr_table_entry_recycler), obj);
}

static void pcc_lr_table__initialize(pcc_context_t *ctx, pcc_lr_table_t *obj) {
    obj->o = 0;
    obj->m = 0;
    obj->n = 0;
    obj->p = NULL;
}

static void pcc_lr_table__finalize(pcc_context_t *ctx, pcc_lr_table_t *obj) {
    while (obj->n > obj->o) {
        obj->n--;
        pcc_lr_table_entry__destroy(ctx, obj->p[obj->n]);
    }
    PCC_FREE(ctx->auxil, obj->p);
}

static void pcc_lr_table__resize(pcc_context_t *ctx, pcc_lr_table_t *obj, size_t len) {
    size_t i;
    for (i = len; i < obj->n; i++) pcc_lr_table_entry__destroy(ctx, obj->p[i]);
    if (obj->m < len) {
        size_t m = obj->m;
        if (m == 0) m = PCC_ARRAY_MIN_SIZE;
        while (m < len && m != 0) m <<= 1;
        if (m == 0) m = len;
        obj->p = (pcc_lr_table_entry_t **)PCC_REALLOC(ctx->auxil, obj->p, sizeof(pcc_lr_table_entry_t *) * m);
        obj->m = m;
    }
    for (i = obj->n; i < len; i++) obj->p[i] = NULL;
    obj->n = len;
}

static void pcc_lr_table__set_head(pcc_context_t *ctx, pcc_lr_table_t *obj, size_t index, pcc_lr_head_t *head) {
    index += obj->o;
    if (index >= obj->n) pcc_lr_table__resize(ctx, obj, index + 1);
    if (obj->p[index] == NULL) obj->p[index] = pcc_lr_table_entry__create(ctx);
    obj->p[index]->head = head;
}

static void pcc_lr_table__hold_head(pcc_context_t *ctx, pcc_lr_table_t *obj, size_t index, pcc_lr_head_t *head) {
    index += obj->o;
    if (index >= obj->n) pcc_lr_table__resize(ctx, obj, index + 1);
    if (obj->p[index] == NULL) obj->p[index] = pcc_lr_table_entry__create(ctx);
    head->hold = obj->p[index]->hold_h;
    obj->p[index]->hold_h = head;
}

static void pcc_lr_table__set_answer(pcc_context_t *ctx, pcc_lr_table_t *obj, size_t index, pcc_rule_t rule, pcc_lr_answer_t *answer) {
    index += obj->o;
    if (index >= obj->n) pcc_lr_table__resize(ctx, obj, index + 1);
    if (obj->p[index] == NULL) obj->p[index] = pcc_lr_table_entry__create(ctx);
    pcc_lr_memo_map__put(ctx, &(obj->p[index]->memos), rule, answer);
}

static void pcc_lr_table__hold_answer(pcc_context_t *ctx, pcc_lr_table_t *obj, size_t index, pcc_lr_answer_t *answer) {
    index += obj->o;
    if (index >= obj->n) pcc_lr_table__resize(ctx, obj, index + 1);
    if (obj->p[index] == NULL) obj->p[index] = pcc_lr_table_entry__create(ctx);
    answer->hold = obj->p[index]->hold_a;
    obj->p[index]->hold_a = answer;
}

static pcc_lr_head_t *pcc_lr_table__get_head(pcc_context_t *ctx, pcc_lr_table_t *obj, size_t index) {
    index += obj->o;
    if (index >= obj->n || obj->p[index] == NULL) return NULL;
    return obj->p[index]->head;
}

static pcc_lr_answer_t *pcc_lr_table__get_answer(pcc_context_t *ctx, pcc_lr_table_t *obj, size_t index, pcc_rule_t rule) {
    index += obj->o;
    if (index >= obj->n || obj->p[index] == NULL) return NULL;
    return pcc_lr_memo_map__get(ctx, &(obj->p[index]->memos), rule);
}

static void pcc_lr_table__shift(pcc_context_t *ctx, pcc_lr_table_t *obj, size_t count) {
    size_t i;
    if (count > obj->n - obj->o) count = obj->n - obj->o;
    for (i = 0; i < count; i++) pcc_lr_table_entry__destroy(ctx, obj->p[obj->o++]);
    if (obj->o > (obj->m >> 1)) {
        memmove(obj->p, obj->p + obj->o, sizeof(pcc_lr_table_entry_t *) * (obj->n - obj->o));
        obj->n -= obj->o;
        obj->o = 0;
    }
}

static pcc_lr_entry_t *pcc_lr_entry__create(pcc_context_t *ctx, pcc_rule_t rule) {
    pcc_lr_entry_t *const lr = (pcc_lr_entry_t *)pcc_memory_recycler__supply(ctx->auxil, &(ctx->lr_entry_recycler));
    lr->rule = rule;
    lr->seed = NULL;
    lr->head = NULL;
    return lr;
}

static void pcc_lr_entry__destroy(pcc_context_t *ctx, pcc_lr_entry_t *lr) {
    pcc_memory_recycler__recycle(ctx->auxil, &(ctx->lr_entry_recycler), lr);
}

static void pcc_lr_stack__initialize(pcc_auxil_t auxil, pcc_lr_stack_t *obj) {
    obj->m = 0;
    obj->n = 0;
    obj->p = NULL;
}

static void pcc_lr_stack__finalize(pcc_auxil_t auxil, pcc_lr_stack_t *obj) {
    PCC_FREE(auxil, obj->p);
}

static void pcc_lr_stack__push(pcc_auxil_t auxil, pcc_lr_stack_t *obj, pcc_lr_entry_t *lr) {
    if (obj->m <= obj->n) {
        const size_t n = obj->n + 1;
        size_t m = obj->m;
        if (m == 0) m = PCC_ARRAY_MIN_SIZE;
        while (m < n && m != 0) m <<= 1;
        if (m == 0) m = n;
        obj->p = (pcc_lr_entry_t **)PCC_REALLOC(auxil, obj->p, sizeof(pcc_lr_entry_t *) * m);
        obj->m = m;
    }
    obj->p[obj->n++] = lr;
}

static pcc_lr_entry_t *pcc_lr_stack__pop(pcc_auxil_t auxil, pcc_lr_stack_t *obj) {
    return obj->p[--obj->n];
}

static pcc_context_t *pcc_context__create(pcc_auxil_t auxil) {
    pcc_context_t *const ctx = (pcc_context_t *)PCC_MALLOC(auxil, sizeof(pcc_context_t));
    ctx->pos = 0;
    ctx->cur = 0;
    ctx->level = 0;
    pcc_char_array__initialize(auxil, &(ctx->buffer));
    pcc_lr_table__initialize(ctx, &(ctx->lrtable));
    pcc_lr_stack__initialize(auxil, &(ctx->lrstack));
    pcc_thunk_array__initialize(ctx, &(ctx->thunks));
    pcc_capture__initialize(ctx->auxil, &(ctx->capt0));
    pcc_memory_recycler__initialize(auxil, &(ctx->thunk_recycler), sizeof(pcc_thunk_t));
    pcc_memory_recycler__initialize(auxil, &(ctx->thunk_chunk_recycler), sizeof(pcc_thunk_chunk_t));
    pcc_memory_recycler__initialize(auxil, &(ctx->lr_head_recycler), sizeof(pcc_lr_head_t));
    pcc_memory_recycler__initialize(auxil, &(ctx->lr_answer_recycler), sizeof(pcc_lr_answer_t));
    pcc_memory_recycler__initialize(auxil, &(ctx->lr_table_entry_recycler), sizeof(pcc_lr_table_entry_t));
    pcc_memory_recycler__initialize(auxil, &(ctx->lr_entry_recycler), sizeof(pcc_lr_entry_t));
    ctx->auxil = auxil;
    return ctx;
}

static void pcc_context__destroy(pcc_context_t *ctx) {
    if (ctx == NULL) return;
    pcc_char_array__finalize(ctx->auxil, &(ctx->buffer));
    pcc_lr_table__finalize(ctx, &(ctx->lrtable));
    pcc_lr_stack__finalize(ctx->auxil, &(ctx->lrstack));
    pcc_thunk_array__finalize(ctx, &(ctx->thunks));
    pcc_capture__finalize(ctx->auxil, &(ctx->capt0));
    pcc_memory_recycler__finalize(ctx->auxil, &(ctx->thunk_recycler));
    pcc_memory_recycler__finalize(ctx->auxil, &(ctx->thunk_chunk_recycler));
    pcc_memory_recycler__finalize(ctx->auxil, &(ctx->lr_head_recycler));
    pcc_memory_recycler__finalize(ctx->auxil, &(ctx->lr_answer_recycler));
    pcc_memory_recycler__finalize(ctx->auxil, &(ctx->lr_table_entry_recycler));
    pcc_memory_recycler__finalize(ctx->auxil, &(ctx->lr_entry_recycler));
    PCC_FREE(ctx->auxil, ctx);
}

static size_t pcc_refill_buffer(pcc_context_t *ctx, size_t num) {
    if (ctx->buffer.n >= ctx->cur + num) return ctx->buffer.n - ctx->cur;
    while (ctx->buffer.n < ctx->cur + num) {
        const int c = PCC_GETCHAR(ctx->auxil);
        if (c < 0) break;
        pcc_char_array__add(ctx->auxil, &(ctx->buffer), (char)c);
    }
    return ctx->buffer.n - ctx->cur;
}

static void pcc_commit_buffer(pcc_context_t *ctx) {
    if (ctx->cur < ctx->buffer.n)
        memmove(ctx->buffer.p, ctx->buffer.p + ctx->cur, ctx->buffer.n - ctx->cur);
    ctx->buffer.n -= ctx->cur;
    ctx->pos += ctx->cur;
    pcc_lr_table__shift(ctx, &(ctx->lrtable), ctx->cur);
    ctx->cur = 0;
}

static const char *pcc_get_capture_string(pcc_context_t *ctx, const pcc_capture_t *capt) {
    if (capt->string.n == 0) {
        const size_t n = capt->range.end - capt->range.start;
        pcc_capture_t *const p = (pcc_capture_t *)capt;
        pcc_char_array__resize(ctx->auxil, &(p->string), n + 1);
        if (n > 0) memcpy(p->string.p, ctx->buffer.p + capt->range.start, n);
        p->string.p[n] = '\0';
    }
    return capt->string.p;
}

static size_t pcc_get_char_as_utf32(pcc_context_t *ctx, int *out) { /* with checking UTF-8 validity */
    int c, u;
    size_t n, i;
    if (pcc_refill_buffer(ctx, 1) < 1) return 0;
    c = (int)(unsigned char)ctx->buffer.p[ctx->cur];
    n = (c < 0x80) ? 1 :
        ((c & 0xe0) == 0xc0) ? 2 :
        ((c & 0xf0) == 0xe0) ? 3 :
        ((c & 0xf8) == 0xf0) ? 4 :
        ((c & 0xfc) == 0xf8) ? 5 :
        ((c & 0xfe) == 0xfc) ? 6 : 0;
    if (n < 1) return 0;
    if (pcc_refill_buffer(ctx, n) < n) return 0;
    u = (n <= 1) ? c : (c & ((1 << (7 - n)) - 1));
    for (i = 1; i < n; i++) {
        const int d = (int)(unsigned char)ctx->buffer.p[ctx->cur + i];
        if ((d & 0xc0) != 0x80) return 0; /* not a continuation byte */
        u = (u << 6) | (d & 0x3f);
    }
    if (
        (n == 2 && u < 0x00000080) ||
        (n == 3 && u < 0x00000800) ||
        (n == 4 && u < 0x00010000) ||
        (n == 5 && u < 0x00200000) ||
        (n == 6 && u < 0x04000000)
    ) return 0; /* invalidly encoded */
    if (out) *out = u;
    return n;
}

static pcc_bool_t pcc_apply_rule(pcc_context_t *ctx, pcc_rule_t rule, pcc_thunk_array_t *thunks, pcc_value_t *value) {
    static pcc_value_t null;
    pcc_thunk_chunk_t *c = NULL;
    const size_t p = ctx->pos + ctx->cur;
    pcc_bool_t b = PCC_TRUE;
    pcc_lr_answer_t *a = pcc_lr_table__get_answer(ctx, &(ctx->lrtable), p, rule);
    pcc_lr_head_t *h = pcc_lr_table__get_head(ctx, &(ctx->lrtable), p);
    if (h != NULL) {
        if (a == NULL && rule != h->rule && pcc_rule_set__index(ctx->auxil, &(h->invol), rule) == PCC_VOID_VALUE) {
            b = PCC_FALSE;
            c = NULL;
        }
        else if (pcc_rule_set__remove(ctx->auxil, &(h->eval), rule)) {
            b = PCC_FALSE;
            c = rule(ctx);
            a = pcc_lr_answer__create(ctx, PCC_LR_ANSWER_CHUNK, ctx->pos + ctx->cur);
            a->data.chunk = c;
            pcc_lr_table__hold_answer(ctx, &(ctx->lrtable), p, a);
        }
    }
    if (b) {
        if (a != NULL) {
            ctx->cur = a->pos - ctx->pos;
            switch (a->type) {
            case PCC_LR_ANSWER_LR:
                if (a->data.lr->head == NULL) {
                    a->data.lr->head = pcc_lr_head__create(ctx, rule);
                    pcc_lr_table__hold_head(ctx, &(ctx->lrtable), p, a->data.lr->head);
                }
                {
                    size_t i = ctx->lrstack.n;
                    while (i > 0) {
                        i--;
                        if (ctx->lrstack.p[i]->head == a->data.lr->head) break;
                        ctx->lrstack.p[i]->head = a->data.lr->head;
                        pcc_rule_set__add(ctx->auxil, &(a->data.lr->head->invol), ctx->lrstack.p[i]->rule);
                    }
                }
                c = a->data.lr->seed;
                break;
            case PCC_LR_ANSWER_CHUNK:
                c = a->data.chunk;
                break;
            default: /* unknown */
                break;
            }
        }
        else {
            pcc_lr_entry_t *const e = pcc_lr_entry__create(ctx, rule);
            pcc_lr_stack__push(ctx->auxil, &(ctx->lrstack), e);
            a = pcc_lr_answer__create(ctx, PCC_LR_ANSWER_LR, p);
            a->data.lr = e;
            pcc_lr_table__set_answer(ctx, &(ctx->lrtable), p, rule, a);
            c = rule(ctx);
            pcc_lr_stack__pop(ctx->auxil, &(ctx->lrstack));
            a->pos = ctx->pos + ctx->cur;
            if (e->head == NULL) {
                pcc_lr_answer__set_chunk(ctx, a, c);
            }
            else {
                e->seed = c;
                h = a->data.lr->head;
                if (h->rule != rule) {
                    c = a->data.lr->seed;
                    a = pcc_lr_answer__create(ctx, PCC_LR_ANSWER_CHUNK, ctx->pos + ctx->cur);
                    a->data.chunk = c;
                    pcc_lr_table__hold_answer(ctx, &(ctx->lrtable), p, a);
                }
                else {
                    pcc_lr_answer__set_chunk(ctx, a, a->data.lr->seed);
                    if (a->data.chunk == NULL) {
                        c = NULL;
                    }
                    else {
                        pcc_lr_table__set_head(ctx, &(ctx->lrtable), p, h);
                        for (;;) {
                            ctx->cur = p - ctx->pos;
                            pcc_rule_set__copy(ctx->auxil, &(h->eval), &(h->invol));
                            c = rule(ctx);
                            if (c == NULL || ctx->pos + ctx->cur <= a->pos) break;
                            pcc_lr_answer__set_chunk(ctx, a, c);
                            a->pos = ctx->pos + ctx->cur;
                        }
                        pcc_thunk_chunk__destroy(ctx, c);
                        pcc_lr_table__set_head(ctx, &(ctx->lrtable), p, NULL);
                        ctx->cur = a->pos - ctx->pos;
                        c = a->data.chunk;
                    }
                }
            }
        }
    }
    if (c == NULL) return PCC_FALSE;
    if (value == NULL) value = &null;
    memset(value, 0, sizeof(pcc_value_t)); /* in case */
    pcc_thunk_array__add(ctx, thunks, pcc_thunk__create_node(ctx, &(c->thunks), value));
    return PCC_TRUE;
}

static void pcc_do_action(pcc_context_t *ctx, const pcc_thunk_array_t *thunks, pcc_value_t *value) {
    size_t i;
    for (i = 0; i < thunks->n; i++) {
        pcc_thunk_t *const thunk = thunks->p[i];
        switch (thunk->type) {
        case PCC_THUNK_LEAF:
            thunk->data.leaf.action(ctx, thunk, value);
            break;
        case PCC_THUNK_NODE:
            pcc_do_action(ctx, thunk->data.node.thunks, thunk->data.node.value);
            break;
        default: /* unknown */
            break;
        }
    }
}

MARK_FUNC_AS_USED
static pcc_auxil_t pcc_ref_auxil_(pcc_context_t *ctx) {
    return ctx->auxil;
}

MARK_FUNC_AS_USED
static const char *pcc_ref_capture_0_(pcc_context_t *ctx) {
    return pcc_get_capture_string(ctx, &(ctx->capt0));
}

MARK_FUNC_AS_USED
static size_t pcc_ref_capture_0s_(pcc_context_t *ctx) {
    return ctx->pos + ctx->capt0.range.start;
}

MARK_FUNC_AS_USED
static size_t pcc_ref_capture_0e_(pcc_context_t *ctx) {
    return ctx->pos + ctx->capt0.range.end;
}

MARK_FUNC_AS_USED
static const char *pcc_ref_capture_(pcc_context_t *ctx, pcc_thunk_chunk_t *in, size_t index) {
    return pcc_get_capture_string(ctx, &(in->capts.p[index]));
}

MARK_FUNC_AS_USED
static size_t pcc_ref_capture_s_(pcc_context_t *ctx, pcc_thunk_chunk_t *in, size_t index) {
    return ctx->pos + in->capts.p[index].range.start;
}

MARK_FUNC_AS_USED
static size_t pcc_ref_capture_e_(pcc_context_t *ctx, pcc_thunk_chunk_t *in, size_t index) {
    return ctx->pos + in->capts.p[index].range.end;
}

MARK_FUNC_AS_USED
static const char *pcc_ref_leaf_capture_0_(pcc_context_t *ctx, pcc_thunk_t *in) {
    return pcc_get_capture_string(ctx, &(in->data.leaf.capt0));
}

MARK_FUNC_AS_USED
static size_t pcc_ref_leaf_capture_0s_(pcc_context_t *ctx, pcc_thunk_t *in) {
    return ctx->pos + in->data.leaf.capt0.range.start;
}

MARK_FUNC_AS_USED
static size_t pcc_ref_leaf_capture_0e_(pcc_context_t *ctx, pcc_thunk_t *in) {
    return ctx->pos + in->data.leaf.capt0.range.end;
}

MARK_FUNC_AS_USED
static const char *pcc_ref_leaf_capture_(pcc_context_t *ctx, pcc_thunk_t *in, size_t index) {
    return pcc_get_capture_string(ctx, in->data.leaf.capts.p[index]);
}

MARK_FUNC_AS_USED
static size_t pcc_ref_leaf_capture_s_(pcc_context_t *ctx, pcc_thunk_t *in, size_t index) {
    return ctx->pos + in->data.leaf.capts.p[index]->range.start;
}

MARK_FUNC_AS_USED
static size_t pcc_ref_leaf_capture_e_(pcc_context_t *ctx, pcc_thunk_t *in, size_t index) {
    return ctx->pos + in->data.leaf.capts.p[index]->range.end;
}

MARK_FUNC_AS_USED
static pcc_value_t *pcc_ref_leaf_variable_(pcc_context_t *ctx, pcc_thunk_t *in, size_t index) {
    return in->data.leaf.values.p[index];
}

static void pcc_action_Programa_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
    if (BACKEND_TARGET == 1) {
    // Si nunca se abrió el main (ej. un script vacío o pura librería), lo abrimos y cerramos
    if (!MAIN_ABIERTO) printf("int main() {\n");
    printf("    return 0;\n}\n");
    }
    pcc_action_out = strdup("");
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static void pcc_action_BloqueInlineLua_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
#define pcc_capture__1 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 0)
    if (BACKEND_TARGET == 0) printf("%s\n", pcc_capture__1);
    pcc_action_out = strdup("");
#undef pcc_capture__1e
#undef pcc_capture__1s
#undef pcc_capture__1
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static void pcc_action_BloqueInlineC_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
#define pcc_capture__1 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 0)
    if (BACKEND_TARGET == 1) printf("%s\n", pcc_capture__1);
    pcc_action_out = strdup("");
#undef pcc_capture__1e
#undef pcc_capture__1s
#undef pcc_capture__1
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static void pcc_action_Primario_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
#define pcc_capture__1 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 0)
    pcc_action_out = strdup(pcc_capture__1);
#undef pcc_capture__1e
#undef pcc_capture__1s
#undef pcc_capture__1
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static void pcc_action_AccesArreglo_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
#define pcc_capture__1 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 0)
#define pcc_capture__2 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 1)
#define pcc_capture__2s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 1)
#define pcc_capture__2e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 1)
    // FIX 3: solo hay 2 capturas (Identificador, Expresion) => $1 y $2, no $3
    // FIX 7: sin buffer estático ni VLA en el stack; tamaño exacto vía snprintf + heap
    int len = snprintf(NULL, 0, "%s[%s]", pcc_capture__1, pcc_capture__2);
    char* buf = malloc(len + 1);
    sprintf(buf, "%s[%s]", pcc_capture__1, pcc_capture__2);
    pcc_action_out = strdup(buf);
    free(buf);
#undef pcc_capture__2e
#undef pcc_capture__2s
#undef pcc_capture__2
#undef pcc_capture__1e
#undef pcc_capture__1s
#undef pcc_capture__1
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static void pcc_action_DirectivaAdd_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
#define pcc_capture__1 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 0)
    // FIX 7: sin buffer estático; tamaño exacto vía snprintf + heap
    // FIX 9: el backend Lua ahora también compila la dependencia de forma recursiva
    // (antes solo emitía el require() asumiendo que el .lua ya existía por otro lado)
    char* ext = (BACKEND_TARGET == 1) ? "h" : "lua";
    char* flag = (BACKEND_TARGET == 1) ? "--c" : "--lua";
    int cmd_len = snprintf(NULL, 0, "./axoc %s < %s.axo > %s.%s", flag, pcc_capture__1, pcc_capture__1, ext);
    char* cmd = malloc(cmd_len + 1);
    sprintf(cmd, "./axoc %s < %s.axo > %s.%s", flag, pcc_capture__1, pcc_capture__1, ext);

    if (BACKEND_TARGET == 1){
    printf("#include \"%s.h\"\n", pcc_capture__1);
    } else {
    printf("local %s = require(\"%s\")\n", pcc_capture__1, pcc_capture__1);
    }
    system(cmd);
    free(cmd);
    pcc_action_out = strdup("");
#undef pcc_capture__1e
#undef pcc_capture__1s
#undef pcc_capture__1
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static void pcc_action_DeclVariable_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
#define pcc_capture__1 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 0)
#define pcc_capture__2 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 1)
#define pcc_capture__2s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 1)
#define pcc_capture__2e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 1)
#define pcc_capture__3 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 2)
#define pcc_capture__3s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 2)
#define pcc_capture__3e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 2)
    if (BACKEND_TARGET == 0) {
    printf("local %s = %s\n", pcc_capture__2, pcc_capture__3);
    } else {
    asegurar_main();
    printf("    auto %s = %s;\n", pcc_capture__2, pcc_capture__3);
    }
    pcc_action_out = strdup("");
#undef pcc_capture__3e
#undef pcc_capture__3s
#undef pcc_capture__3
#undef pcc_capture__2e
#undef pcc_capture__2s
#undef pcc_capture__2
#undef pcc_capture__1e
#undef pcc_capture__1s
#undef pcc_capture__1
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static void pcc_action_DeclArreglo_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
#define pcc_capture__1 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 0)
#define pcc_capture__2 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 1)
#define pcc_capture__2s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 1)
#define pcc_capture__2e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 1)
#define pcc_capture__3 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 2)
#define pcc_capture__3s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 2)
#define pcc_capture__3e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 2)
    if (BACKEND_TARGET == 0) {
    printf("local %s = {%s}\n", pcc_capture__2, pcc_capture__3);
    } else {
    asegurar_main();

    char* tipo_c = "double";
    if (strcmp(pcc_capture__1, "dec") == 0) tipo_c = "_Decimal128";
    else if (strcmp(pcc_capture__1, "int") == 0) tipo_c = "int";
    else if (strcmp(pcc_capture__1, "flt") == 0) tipo_c = "double";
    else if (strcmp(pcc_capture__1, "chara") == 0) tipo_c = "char";
    else if (strcmp(pcc_capture__1, "com") == 0) tipo_c = "double complex";
    printf("    %s %s[] = {%s};\n", tipo_c, pcc_capture__2, pcc_capture__3);
    }
    pcc_action_out = strdup("");
#undef pcc_capture__3e
#undef pcc_capture__3s
#undef pcc_capture__3
#undef pcc_capture__2e
#undef pcc_capture__2s
#undef pcc_capture__2
#undef pcc_capture__1e
#undef pcc_capture__1s
#undef pcc_capture__1
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static void pcc_action_InstruccionExpresion_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
#define pcc_capture__1 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 0)
    if (BACKEND_TARGET == 0) {
    printf("%s\n", pcc_capture__1);
    } else {
    asegurar_main();
    printf("    %s;\n", pcc_capture__1);
    }
    pcc_action_out = strdup("");
#undef pcc_capture__1e
#undef pcc_capture__1s
#undef pcc_capture__1
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static void pcc_action_ListaElementos_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
#define pcc_capture__1 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 0)
    pcc_action_out = strdup(pcc_capture__1);
#undef pcc_capture__1e
#undef pcc_capture__1s
#undef pcc_capture__1
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static void pcc_action_Expresion_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
#define pcc_capture__1 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 0)
    pcc_action_out = strdup(pcc_capture__1);
#undef pcc_capture__1e
#undef pcc_capture__1s
#undef pcc_capture__1
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static void pcc_action_Termino_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
#define pcc_capture__1 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 0)
    pcc_action_out = strdup(pcc_capture__1);
#undef pcc_capture__1e
#undef pcc_capture__1s
#undef pcc_capture__1
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static void pcc_action_LlamadaFuncion_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
#define pcc_capture__1 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 0)
#define pcc_capture__2 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 1)
#define pcc_capture__2s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 1)
#define pcc_capture__2e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 1)
    // FIX 7: sin buffer estático; tamaño exacto vía snprintf + heap
    const char* args = pcc_capture__2 ? pcc_capture__2 : "";
    int len = snprintf(NULL, 0, "%s(%s)", pcc_capture__1, args);
    char* buf = malloc(len + 1);
    sprintf(buf, "%s(%s)", pcc_capture__1, args);
    pcc_action_out = strdup(buf);
    free(buf);
#undef pcc_capture__2e
#undef pcc_capture__2s
#undef pcc_capture__2
#undef pcc_capture__1e
#undef pcc_capture__1s
#undef pcc_capture__1
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static void pcc_action_Factor_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
#define pcc_capture__1 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 0)
    pcc_action_out = strdup(pcc_capture__1);
#undef pcc_capture__1e
#undef pcc_capture__1s
#undef pcc_capture__1
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static void pcc_action_Literal_0(axolang_context_t *pcc_ctx, pcc_thunk_t *pcc_in, pcc_value_t *pcc_out) {
#define auxil pcc_ref_auxil_(pcc_ctx)
#define pcc_action_out (*pcc_out)
#define pcc_capture__0 pcc_ref_leaf_capture_0_(pcc_ctx, pcc_in)
#define pcc_capture__0s pcc_ref_leaf_capture_0s_(pcc_ctx, pcc_in)
#define pcc_capture__0e pcc_ref_leaf_capture_0e_(pcc_ctx, pcc_in)
#define pcc_capture__1 pcc_ref_leaf_capture_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1s pcc_ref_leaf_capture_s_(pcc_ctx, pcc_in, 0)
#define pcc_capture__1e pcc_ref_leaf_capture_e_(pcc_ctx, pcc_in, 0)
    pcc_action_out = strdup(pcc_capture__1);
#undef pcc_capture__1e
#undef pcc_capture__1s
#undef pcc_capture__1
#undef pcc_capture__0e
#undef pcc_capture__0s
#undef pcc_capture__0
#undef pcc_action_out
#undef auxil
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_Programa(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SeccionScript(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_SeccionUI(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_Declaracion(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_DefFuncion(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_BloqueInline(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_BloqueInlineLua(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_BloqueInlineC(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_Primario(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_AccesArreglo(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_DirectivaAdd(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_DeclVariable(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_DeclArreglo(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_InstruccionExpresion(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ListaElementos(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_Expresion(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_Termino(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_LlamadaFuncion(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_Factor(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_Literal(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_Tipo(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_Identificador(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NumComplejo(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NumDecimal(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NumEntero(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_Cadena(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_Caracter(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ComponenteUI(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_Atributo(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule__(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_Comentario(pcc_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_EOF(pcc_context_t *ctx);

static pcc_thunk_chunk_t *pcc_evaluate_rule_Programa(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "Programa", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SeccionScript, &(chunk->thunks), NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_SeccionUI, &(chunk->thunks), NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EOF, &(chunk->thunks), NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_Programa_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
        pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "Programa", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "Programa", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SeccionScript(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "SeccionScript", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != '#' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 's' ||
        pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != 'c' ||
        pcc_refill_buffer(ctx, 4) < 4 || (ctx->buffer.p + ctx->cur)[3] != 'r' ||
        pcc_refill_buffer(ctx, 5) < 5 || (ctx->buffer.p + ctx->cur)[4] != 'i' ||
        pcc_refill_buffer(ctx, 6) < 6 || (ctx->buffer.p + ctx->cur)[5] != 'p' ||
        pcc_refill_buffer(ctx, 7) < 7 || (ctx->buffer.p + ctx->cur)[6] != 't'
    ) goto L0000;
    ctx->cur += 7;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        for (;;) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DirectivaAdd, &(chunk->thunks), NULL)) goto L0001;
            if (ctx->cur == p) break;
            continue;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
    }
    {
        for (;;) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Declaracion, &(chunk->thunks), NULL)) goto L0002;
            if (ctx->cur == p) break;
            continue;
        L0002:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "SeccionScript", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "SeccionScript", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_SeccionUI(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "SeccionUI", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.n;
        if (
            pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != '#' ||
            pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 'u' ||
            pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != 'i'
        ) goto L0002;
        ctx->cur += 3;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0002;
        {
            for (;;) {
                const size_t p = ctx->cur;
                MARK_VAR_AS_USED
                const size_t n = chunk->thunks.n;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ComponenteUI, &(chunk->thunks), NULL)) goto L0003;
                if (ctx->cur == p) break;
                continue;
            L0003:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
                break;
            }
        }
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "SeccionUI", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_Declaracion(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "Declaracion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.n;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DefFuncion, &(chunk->thunks), NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DeclArreglo, &(chunk->thunks), NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DeclVariable, &(chunk->thunks), NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_BloqueInline, &(chunk->thunks), NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_InstruccionExpresion, &(chunk->thunks), NULL)) goto L0006;
        goto L0001;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        goto L0000;
    L0001:;
    }
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.n;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.p[ctx->cur] != ';'
        ) goto L0008;
        ctx->cur++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0008;
        goto L0007;
    L0008:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0009;
        goto L0007;
    L0009:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        goto L0000;
    L0007:;
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "Declaracion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "Declaracion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_DefFuncion(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "DefFuncion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != 'f' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 'u' ||
        pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != 'n'
    ) goto L0000;
    ctx->cur += 3;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        const size_t p0 = ctx->cur;
        const size_t n0 = chunk->thunks.n;
        int i;
        for (i = 0;; i++) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0001;
                if (!(
                    (u >= 0x000061 && u <= 0x00007a) ||
                    (u >= 0x000041 && u <= 0x00005a) ||
                    (u >= 0x000030 && u <= 0x000039) ||
                    u == 0x00005f
                )) goto L0001;
                ctx->cur += n;
            }
            if (ctx->cur == p) break;
            continue;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
        if (i < 1) {
            ctx->cur = p0;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n0);
            goto L0000;
        }
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '='
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != '(' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != ')'
    ) goto L0000;
    ctx->cur += 2;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '{'
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        for (;;) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.n;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_BloqueInline, &(chunk->thunks), NULL)) goto L0004;
                goto L0003;
            L0004:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0005;
                goto L0003;
            L0005:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
                goto L0002;
            L0003:;
            }
            if (ctx->cur == p) break;
            continue;
        L0002:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '}'
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "DefFuncion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "DefFuncion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_BloqueInline(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "BloqueInline", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.n;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_BloqueInlineLua, &(chunk->thunks), NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_BloqueInlineC, &(chunk->thunks), NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "BloqueInline", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "BloqueInline", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_BloqueInlineLua(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "BloqueInlineLua", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 1);
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != 'i' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 'n' ||
        pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != 'l' ||
        pcc_refill_buffer(ctx, 4) < 4 || (ctx->buffer.p + ctx->cur)[3] != 'i' ||
        pcc_refill_buffer(ctx, 5) < 5 || (ctx->buffer.p + ctx->cur)[4] != 'n' ||
        pcc_refill_buffer(ctx, 6) < 6 || (ctx->buffer.p + ctx->cur)[5] != 'e' ||
        pcc_refill_buffer(ctx, 7) < 7 || (ctx->buffer.p + ctx->cur)[6] != '_' ||
        pcc_refill_buffer(ctx, 8) < 8 || (ctx->buffer.p + ctx->cur)[7] != 'l' ||
        pcc_refill_buffer(ctx, 9) < 9 || (ctx->buffer.p + ctx->cur)[8] != 'u' ||
        pcc_refill_buffer(ctx, 10) < 10 || (ctx->buffer.p + ctx->cur)[9] != 'a'
    ) goto L0000;
    ctx->cur += 10;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != '\xc2' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != '\xab'
    ) goto L0000;
    ctx->cur += 2;
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            for (;;) {
                const size_t p = ctx->cur;
                MARK_VAR_AS_USED
                const size_t n = chunk->thunks.n;
                {
                    int u;
                    const size_t n = pcc_get_char_as_utf32(ctx, &u);
                    if (n == 0) goto L0001;
                    if (
                        u == 0x0000bb
                    ) goto L0001;
                    ctx->cur += n;
                }
                if (ctx->cur == p) break;
                continue;
            L0001:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
                break;
            }
        }
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != '\xc2' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != '\xbb'
    ) goto L0000;
    ctx->cur += 2;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_BloqueInlineLua_0, 0, 1);
        thunk->data.leaf.capts.p[0] = &(chunk->capts.p[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
        pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "BloqueInlineLua", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "BloqueInlineLua", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_BloqueInlineC(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "BloqueInlineC", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 1);
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != 'i' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 'n' ||
        pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != 'l' ||
        pcc_refill_buffer(ctx, 4) < 4 || (ctx->buffer.p + ctx->cur)[3] != 'i' ||
        pcc_refill_buffer(ctx, 5) < 5 || (ctx->buffer.p + ctx->cur)[4] != 'n' ||
        pcc_refill_buffer(ctx, 6) < 6 || (ctx->buffer.p + ctx->cur)[5] != 'e' ||
        pcc_refill_buffer(ctx, 7) < 7 || (ctx->buffer.p + ctx->cur)[6] != '_' ||
        pcc_refill_buffer(ctx, 8) < 8 || (ctx->buffer.p + ctx->cur)[7] != 'c'
    ) goto L0000;
    ctx->cur += 8;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != '\xc2' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != '\xab'
    ) goto L0000;
    ctx->cur += 2;
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            for (;;) {
                const size_t p = ctx->cur;
                MARK_VAR_AS_USED
                const size_t n = chunk->thunks.n;
                {
                    int u;
                    const size_t n = pcc_get_char_as_utf32(ctx, &u);
                    if (n == 0) goto L0001;
                    if (
                        u == 0x0000bb
                    ) goto L0001;
                    ctx->cur += n;
                }
                if (ctx->cur == p) break;
                continue;
            L0001:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
                break;
            }
        }
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != '\xc2' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != '\xbb'
    ) goto L0000;
    ctx->cur += 2;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_BloqueInlineC_0, 0, 1);
        thunk->data.leaf.capts.p[0] = &(chunk->capts.p[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
        pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "BloqueInlineC", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "BloqueInlineC", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_Primario(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "Primario", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 1);
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.n;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_LlamadaFuncion, &(chunk->thunks), NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_AccesArreglo, &(chunk->thunks), NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        {
            const size_t p = ctx->cur;
            size_t q;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Factor, &(chunk->thunks), NULL)) goto L0004;
            q = ctx->cur;
            chunk->capts.p[0].range.start = p;
            chunk->capts.p[0].range.end = q;
            pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
        }
        {
            pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_Primario_0, 0, 1);
            thunk->data.leaf.capts.p[0] = &(chunk->capts.p[0]);
            thunk->data.leaf.capt0.range.start = chunk->pos;
            thunk->data.leaf.capt0.range.end = ctx->cur;
            pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
            pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
        }
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "Primario", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "Primario", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_AccesArreglo(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "AccesArreglo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 2);
    {
        const size_t p = ctx->cur;
        size_t q;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Identificador, &(chunk->thunks), NULL)) goto L0000;
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '['
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        size_t q;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Expresion, &(chunk->thunks), NULL)) goto L0000;
        q = ctx->cur;
        chunk->capts.p[1].range.start = p;
        chunk->capts.p[1].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[1].string), 0);
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != ']'
    ) goto L0000;
    ctx->cur++;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_AccesArreglo_0, 0, 2);
        thunk->data.leaf.capts.p[0] = &(chunk->capts.p[0]);
        thunk->data.leaf.capts.p[1] = &(chunk->capts.p[1]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
        pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "AccesArreglo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "AccesArreglo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_DirectivaAdd(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "DirectivaAdd", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 1);
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != 'a' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 'd' ||
        pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != 'd'
    ) goto L0000;
    ctx->cur += 3;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '<'
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            const size_t p0 = ctx->cur;
            const size_t n0 = chunk->thunks.n;
            int i;
            for (i = 0;; i++) {
                const size_t p = ctx->cur;
                MARK_VAR_AS_USED
                const size_t n = chunk->thunks.n;
                {
                    int u;
                    const size_t n = pcc_get_char_as_utf32(ctx, &u);
                    if (n == 0) goto L0001;
                    if (!(
                        (u >= 0x000061 && u <= 0x00007a) ||
                        (u >= 0x000041 && u <= 0x00005a) ||
                        (u >= 0x000030 && u <= 0x000039) ||
                        u == 0x00005f
                    )) goto L0001;
                    ctx->cur += n;
                }
                if (ctx->cur == p) break;
                continue;
            L0001:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
                break;
            }
            if (i < 1) {
                ctx->cur = p0;
                pcc_thunk_array__revert(ctx, &(chunk->thunks), n0);
                goto L0000;
            }
        }
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != '.' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 'a' ||
        pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != 'x' ||
        pcc_refill_buffer(ctx, 4) < 4 || (ctx->buffer.p + ctx->cur)[3] != 'o'
    ) goto L0000;
    ctx->cur += 4;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '>'
    ) goto L0000;
    ctx->cur++;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_DirectivaAdd_0, 0, 1);
        thunk->data.leaf.capts.p[0] = &(chunk->capts.p[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
        pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "DirectivaAdd", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "DirectivaAdd", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_DeclVariable(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "DeclVariable", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 3);
    {
        const size_t p = ctx->cur;
        size_t q;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Tipo, &(chunk->thunks), NULL)) goto L0000;
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        size_t q;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Identificador, &(chunk->thunks), NULL)) goto L0000;
        q = ctx->cur;
        chunk->capts.p[1].range.start = p;
        chunk->capts.p[1].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[1].string), 0);
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '='
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        size_t q;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Expresion, &(chunk->thunks), NULL)) goto L0000;
        q = ctx->cur;
        chunk->capts.p[2].range.start = p;
        chunk->capts.p[2].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[2].string), 0);
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_DeclVariable_0, 0, 3);
        thunk->data.leaf.capts.p[0] = &(chunk->capts.p[0]);
        thunk->data.leaf.capts.p[1] = &(chunk->capts.p[1]);
        thunk->data.leaf.capts.p[2] = &(chunk->capts.p[2]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
        pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "DeclVariable", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "DeclVariable", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_DeclArreglo(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "DeclArreglo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 3);
    {
        const size_t p = ctx->cur;
        size_t q;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Tipo, &(chunk->thunks), NULL)) goto L0000;
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        size_t q;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Identificador, &(chunk->thunks), NULL)) goto L0000;
        q = ctx->cur;
        chunk->capts.p[1].range.start = p;
        chunk->capts.p[1].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[1].string), 0);
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != '[' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != ']'
    ) goto L0000;
    ctx->cur += 2;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '='
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != '\xe2' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != '\x9f' ||
        pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != '\xa8'
    ) goto L0000;
    ctx->cur += 3;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        size_t q;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ListaElementos, &(chunk->thunks), NULL)) goto L0000;
        q = ctx->cur;
        chunk->capts.p[2].range.start = p;
        chunk->capts.p[2].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[2].string), 0);
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != '\xe2' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != '\x9f' ||
        pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != '\xa9'
    ) goto L0000;
    ctx->cur += 3;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_DeclArreglo_0, 0, 3);
        thunk->data.leaf.capts.p[0] = &(chunk->capts.p[0]);
        thunk->data.leaf.capts.p[1] = &(chunk->capts.p[1]);
        thunk->data.leaf.capts.p[2] = &(chunk->capts.p[2]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
        pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "DeclArreglo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "DeclArreglo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_InstruccionExpresion(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "InstruccionExpresion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 1);
    {
        const size_t p = ctx->cur;
        size_t q;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Expresion, &(chunk->thunks), NULL)) goto L0000;
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_InstruccionExpresion_0, 0, 1);
        thunk->data.leaf.capts.p[0] = &(chunk->capts.p[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
        pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "InstruccionExpresion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "InstruccionExpresion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ListaElementos(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "ListaElementos", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 1);
    {
        const size_t p = ctx->cur;
        size_t q;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Expresion, &(chunk->thunks), NULL)) goto L0000;
        {
            for (;;) {
                const size_t p = ctx->cur;
                MARK_VAR_AS_USED
                const size_t n = chunk->thunks.n;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0001;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.p[ctx->cur] != ','
                ) goto L0001;
                ctx->cur++;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0001;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Expresion, &(chunk->thunks), NULL)) goto L0001;
                if (ctx->cur == p) break;
                continue;
            L0001:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
                break;
            }
        }
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_ListaElementos_0, 0, 1);
        thunk->data.leaf.capts.p[0] = &(chunk->capts.p[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
        pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "ListaElementos", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "ListaElementos", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_Expresion(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "Expresion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 1);
    {
        const size_t p = ctx->cur;
        size_t q;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Termino, &(chunk->thunks), NULL)) goto L0000;
        {
            for (;;) {
                const size_t p = ctx->cur;
                MARK_VAR_AS_USED
                const size_t n = chunk->thunks.n;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0001;
                {
                    int u;
                    const size_t n = pcc_get_char_as_utf32(ctx, &u);
                    if (n == 0) goto L0001;
                    if (!(
                        u == 0x00002b ||
                        u == 0x00002d
                    )) goto L0001;
                    ctx->cur += n;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0001;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Termino, &(chunk->thunks), NULL)) goto L0001;
                if (ctx->cur == p) break;
                continue;
            L0001:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
                break;
            }
        }
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_Expresion_0, 0, 1);
        thunk->data.leaf.capts.p[0] = &(chunk->capts.p[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
        pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "Expresion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "Expresion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_Termino(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "Termino", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 1);
    {
        const size_t p = ctx->cur;
        size_t q;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Primario, &(chunk->thunks), NULL)) goto L0000;
        {
            for (;;) {
                const size_t p = ctx->cur;
                MARK_VAR_AS_USED
                const size_t n = chunk->thunks.n;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0001;
                {
                    int u;
                    const size_t n = pcc_get_char_as_utf32(ctx, &u);
                    if (n == 0) goto L0001;
                    if (!(
                        u == 0x00002a ||
                        u == 0x00002f
                    )) goto L0001;
                    ctx->cur += n;
                }
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0001;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Primario, &(chunk->thunks), NULL)) goto L0001;
                if (ctx->cur == p) break;
                continue;
            L0001:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
                break;
            }
        }
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_Termino_0, 0, 1);
        thunk->data.leaf.capts.p[0] = &(chunk->capts.p[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
        pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "Termino", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "Termino", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_LlamadaFuncion(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "LlamadaFuncion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 2);
    {
        const size_t p = ctx->cur;
        size_t q;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Identificador, &(chunk->thunks), NULL)) goto L0000;
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '('
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            MARK_VAR_AS_USED
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ListaElementos, &(chunk->thunks), NULL)) goto L0001;
            goto L0002;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        L0002:;
        }
        q = ctx->cur;
        chunk->capts.p[1].range.start = p;
        chunk->capts.p[1].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[1].string), 0);
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != ')'
    ) goto L0000;
    ctx->cur++;
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_LlamadaFuncion_0, 0, 2);
        thunk->data.leaf.capts.p[0] = &(chunk->capts.p[0]);
        thunk->data.leaf.capts.p[1] = &(chunk->capts.p[1]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
        pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "LlamadaFuncion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "LlamadaFuncion", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_Factor(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "Factor", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 1);
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.n;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Literal, &(chunk->thunks), NULL)) goto L0002;
            goto L0001;
        L0002:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Identificador, &(chunk->thunks), NULL)) goto L0003;
            goto L0001;
        L0003:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            goto L0000;
        L0001:;
        }
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_Factor_0, 0, 1);
        thunk->data.leaf.capts.p[0] = &(chunk->capts.p[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
        pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "Factor", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "Factor", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_Literal(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "Literal", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 1);
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            const size_t p = ctx->cur;
            const size_t n = chunk->thunks.n;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NumDecimal, &(chunk->thunks), NULL)) goto L0002;
            goto L0001;
        L0002:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NumComplejo, &(chunk->thunks), NULL)) goto L0003;
            goto L0001;
        L0003:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NumEntero, &(chunk->thunks), NULL)) goto L0004;
            goto L0001;
        L0004:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Cadena, &(chunk->thunks), NULL)) goto L0005;
            goto L0001;
        L0005:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Caracter, &(chunk->thunks), NULL)) goto L0006;
            goto L0001;
        L0006:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            goto L0000;
        L0001:;
        }
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    {
        pcc_thunk_t *const thunk = pcc_thunk__create_leaf(ctx, pcc_action_Literal_0, 0, 1);
        thunk->data.leaf.capts.p[0] = &(chunk->capts.p[0]);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->cur;
        pcc_char_array__resize(ctx->auxil, &(thunk->data.leaf.capt0.string), 0);
        pcc_thunk_array__add(ctx, &(chunk->thunks), thunk);
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "Literal", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "Literal", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_Tipo(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "Tipo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.n;
        if (
            pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != 'i' ||
            pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 'n' ||
            pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != 't'
        ) goto L0002;
        ctx->cur += 3;
        goto L0001;
    L0002:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != 'f' ||
            pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 'l' ||
            pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != 't'
        ) goto L0003;
        ctx->cur += 3;
        goto L0001;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != 'd' ||
            pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 'e' ||
            pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != 'c'
        ) goto L0004;
        ctx->cur += 3;
        goto L0001;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != 'b' ||
            pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 'o' ||
            pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != 'o'
        ) goto L0005;
        ctx->cur += 3;
        goto L0001;
    L0005:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != 'c' ||
            pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 'h' ||
            pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != 'a' ||
            pcc_refill_buffer(ctx, 4) < 4 || (ctx->buffer.p + ctx->cur)[3] != 'r' ||
            pcc_refill_buffer(ctx, 5) < 5 || (ctx->buffer.p + ctx->cur)[4] != 'a'
        ) goto L0006;
        ctx->cur += 5;
        goto L0001;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != 'c' ||
            pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 'o' ||
            pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != 'm'
        ) goto L0007;
        ctx->cur += 3;
        goto L0001;
    L0007:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != 'a' ||
            pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 'u' ||
            pcc_refill_buffer(ctx, 3) < 3 || (ctx->buffer.p + ctx->cur)[2] != 't' ||
            pcc_refill_buffer(ctx, 4) < 4 || (ctx->buffer.p + ctx->cur)[3] != 'o'
        ) goto L0008;
        ctx->cur += 4;
        goto L0001;
    L0008:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        goto L0000;
    L0001:;
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "Tipo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "Tipo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_Identificador(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "Identificador", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    {
        int u;
        const size_t n = pcc_get_char_as_utf32(ctx, &u);
        if (n == 0) goto L0000;
        if (!(
            (u >= 0x000061 && u <= 0x00007a) ||
            (u >= 0x000041 && u <= 0x00005a) ||
            u == 0x00005f
        )) goto L0000;
        ctx->cur += n;
    }
    {
        for (;;) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0001;
                if (!(
                    (u >= 0x000061 && u <= 0x00007a) ||
                    (u >= 0x000041 && u <= 0x00005a) ||
                    (u >= 0x000030 && u <= 0x000039) ||
                    u == 0x00005f
                )) goto L0001;
                ctx->cur += n;
            }
            if (ctx->cur == p) break;
            continue;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "Identificador", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "Identificador", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NumComplejo(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "NumComplejo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    {
        const size_t p0 = ctx->cur;
        const size_t n0 = chunk->thunks.n;
        int i;
        for (i = 0;; i++) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0001;
                if (!(
                    (u >= 0x000030 && u <= 0x000039)
                )) goto L0001;
                ctx->cur += n;
            }
            if (ctx->cur == p) break;
            continue;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
        if (i < 1) {
            ctx->cur = p0;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n0);
            goto L0000;
        }
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.n;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.p[ctx->cur] != '+'
        ) goto L0003;
        ctx->cur++;
        goto L0002;
    L0003:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.p[ctx->cur] != '-'
        ) goto L0004;
        ctx->cur++;
        goto L0002;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        goto L0000;
    L0002:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        const size_t p0 = ctx->cur;
        const size_t n0 = chunk->thunks.n;
        int i;
        for (i = 0;; i++) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0005;
                if (!(
                    (u >= 0x000030 && u <= 0x000039) ||
                    u == 0x00002e
                )) goto L0005;
                ctx->cur += n;
            }
            if (ctx->cur == p) break;
            continue;
        L0005:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
        if (i < 1) {
            ctx->cur = p0;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n0);
            goto L0000;
        }
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != 'i'
    ) goto L0000;
    ctx->cur++;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "NumComplejo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "NumComplejo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NumDecimal(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "NumDecimal", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    {
        const size_t p0 = ctx->cur;
        const size_t n0 = chunk->thunks.n;
        int i;
        for (i = 0;; i++) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0001;
                if (!(
                    (u >= 0x000030 && u <= 0x000039)
                )) goto L0001;
                ctx->cur += n;
            }
            if (ctx->cur == p) break;
            continue;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
        if (i < 1) {
            ctx->cur = p0;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n0);
            goto L0000;
        }
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '.'
    ) goto L0000;
    ctx->cur++;
    {
        const size_t p0 = ctx->cur;
        const size_t n0 = chunk->thunks.n;
        int i;
        for (i = 0;; i++) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0002;
                if (!(
                    (u >= 0x000030 && u <= 0x000039)
                )) goto L0002;
                ctx->cur += n;
            }
            if (ctx->cur == p) break;
            continue;
        L0002:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
        if (i < 1) {
            ctx->cur = p0;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n0);
            goto L0000;
        }
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != 'D' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != 'L'
    ) goto L0000;
    ctx->cur += 2;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "NumDecimal", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "NumDecimal", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NumEntero(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "NumEntero", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    {
        const size_t p0 = ctx->cur;
        const size_t n0 = chunk->thunks.n;
        int i;
        for (i = 0;; i++) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0001;
                if (!(
                    (u >= 0x000030 && u <= 0x000039)
                )) goto L0001;
                ctx->cur += n;
            }
            if (ctx->cur == p) break;
            continue;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
        if (i < 1) {
            ctx->cur = p0;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n0);
            goto L0000;
        }
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "NumEntero", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "NumEntero", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_Cadena(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "Cadena", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 1);
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != '\xc2' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != '\xab'
    ) goto L0000;
    ctx->cur += 2;
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            for (;;) {
                const size_t p = ctx->cur;
                MARK_VAR_AS_USED
                const size_t n = chunk->thunks.n;
                {
                    int u;
                    const size_t n = pcc_get_char_as_utf32(ctx, &u);
                    if (n == 0) goto L0001;
                    if (
                        u == 0x0000bb
                    ) goto L0001;
                    ctx->cur += n;
                }
                if (ctx->cur == p) break;
                continue;
            L0001:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
                break;
            }
        }
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != '\xc2' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != '\xbb'
    ) goto L0000;
    ctx->cur += 2;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "Cadena", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "Cadena", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_Caracter(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "Caracter", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    pcc_capture_table__resize(ctx->auxil, &(chunk->capts), 1);
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '\''
    ) goto L0000;
    ctx->cur++;
    {
        const size_t p = ctx->cur;
        size_t q;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0000;
            ctx->cur += n;
        }
        q = ctx->cur;
        chunk->capts.p[0].range.start = p;
        chunk->capts.p[0].range.end = q;
        pcc_char_array__resize(ctx->auxil, &(chunk->capts.p[0].string), 0);
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '\''
    ) goto L0000;
    ctx->cur++;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "Caracter", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "Caracter", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ComponenteUI(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "ComponenteUI", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '<'
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        const size_t p0 = ctx->cur;
        const size_t n0 = chunk->thunks.n;
        int i;
        for (i = 0;; i++) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0001;
                if (!(
                    (u >= 0x000061 && u <= 0x00007a) ||
                    (u >= 0x000041 && u <= 0x00005a) ||
                    u == 0x00005f
                )) goto L0001;
                ctx->cur += n;
            }
            if (ctx->cur == p) break;
            continue;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
        if (i < 1) {
            ctx->cur = p0;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n0);
            goto L0000;
        }
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        for (;;) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Atributo, &(chunk->thunks), NULL)) goto L0002;
            if (ctx->cur == p) break;
            continue;
        L0002:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '>'
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    {
        const size_t p = ctx->cur;
        const size_t n = chunk->thunks.n;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.p[ctx->cur] != '{'
        ) goto L0004;
        ctx->cur++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0004;
        {
            for (;;) {
                const size_t p = ctx->cur;
                MARK_VAR_AS_USED
                const size_t n = chunk->thunks.n;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ComponenteUI, &(chunk->thunks), NULL)) goto L0005;
                if (ctx->cur == p) break;
                continue;
            L0005:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
                break;
            }
        }
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0004;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.p[ctx->cur] != '}'
        ) goto L0004;
        ctx->cur++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0004;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.p[ctx->cur] != ';'
        ) goto L0004;
        ctx->cur++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0004;
        goto L0003;
    L0004:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.p[ctx->cur] != ';'
        ) goto L0006;
        ctx->cur++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0006;
        goto L0003;
    L0006:;
        ctx->cur = p;
        pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
        goto L0000;
    L0003:;
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "ComponenteUI", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "ComponenteUI", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_Atributo(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "Atributo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    {
        const size_t p0 = ctx->cur;
        const size_t n0 = chunk->thunks.n;
        int i;
        for (i = 0;; i++) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0001;
                if (!(
                    (u >= 0x000061 && u <= 0x00007a) ||
                    (u >= 0x000041 && u <= 0x00005a) ||
                    u == 0x00005f ||
                    u == 0x00003a
                )) goto L0001;
                ctx->cur += n;
            }
            if (ctx->cur == p) break;
            continue;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
        if (i < 1) {
            ctx->cur = p0;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n0);
            goto L0000;
        }
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '='
    ) goto L0000;
    ctx->cur++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Cadena, &(chunk->thunks), NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &(chunk->thunks), NULL)) goto L0000;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "Atributo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "Atributo", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule__(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "_", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    {
        for (;;) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            {
                const size_t p = ctx->cur;
                const size_t n = chunk->thunks.n;
                {
                    int u;
                    const size_t n = pcc_get_char_as_utf32(ctx, &u);
                    if (n == 0) goto L0003;
                    if (!(
                        u == 0x000020 ||
                        u == 0x000009 ||
                        u == 0x00000d ||
                        u == 0x00000a
                    )) goto L0003;
                    ctx->cur += n;
                }
                goto L0002;
            L0003:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_Comentario, &(chunk->thunks), NULL)) goto L0004;
                goto L0002;
            L0004:;
                ctx->cur = p;
                pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
                goto L0001;
            L0002:;
            }
            if (ctx->cur == p) break;
            continue;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "_", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_Comentario(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "Comentario", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    if (
        pcc_refill_buffer(ctx, 1) < 1 || (ctx->buffer.p + ctx->cur)[0] != '/' ||
        pcc_refill_buffer(ctx, 2) < 2 || (ctx->buffer.p + ctx->cur)[1] != '/'
    ) goto L0000;
    ctx->cur += 2;
    {
        for (;;) {
            const size_t p = ctx->cur;
            MARK_VAR_AS_USED
            const size_t n = chunk->thunks.n;
            {
                int u;
                const size_t n = pcc_get_char_as_utf32(ctx, &u);
                if (n == 0) goto L0001;
                if (
                    u == 0x00000a
                ) goto L0001;
                ctx->cur += n;
            }
            if (ctx->cur == p) break;
            continue;
        L0001:;
            ctx->cur = p;
            pcc_thunk_array__revert(ctx, &(chunk->thunks), n);
            break;
        }
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.p[ctx->cur] != '\n'
    ) goto L0000;
    ctx->cur++;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "Comentario", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "Comentario", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_EOF(pcc_context_t *ctx) {
    pcc_thunk_chunk_t *const chunk = pcc_thunk_chunk__create(ctx);
    chunk->pos = ctx->cur;
    PCC_DEBUG(ctx->auxil, PCC_DBG_EVALUATE, "EOF", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->buffer.n - chunk->pos);
    ctx->level++;
    {
        const size_t p = ctx->cur;
        {
            int u;
            const size_t n = pcc_get_char_as_utf32(ctx, &u);
            if (n == 0) goto L0001;
            ctx->cur += n;
        }
        ctx->cur = p;
        goto L0000;
    L0001:;
        ctx->cur = p;
    }
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_MATCH, "EOF", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    return chunk;
L0000:;
    ctx->level--;
    PCC_DEBUG(ctx->auxil, PCC_DBG_NOMATCH, "EOF", ctx->level, chunk->pos, ctx->buffer.p + chunk->pos, ctx->cur - chunk->pos);
    pcc_thunk_chunk__destroy(ctx, chunk);
    return NULL;
}

axolang_context_t *axolang_create(void *auxil) {
    return pcc_context__create(auxil);
}

int axolang_parse(axolang_context_t *ctx, char **ret) {
    if (pcc_refill_buffer(ctx, 1) < 1) return 0;
    if (pcc_apply_rule(ctx, pcc_evaluate_rule_Programa, &(ctx->thunks), ret))
        pcc_do_action(ctx, &(ctx->thunks), ret);
    else
        PCC_ERROR(ctx->auxil);
    pcc_commit_buffer(ctx);
    pcc_thunk_array__revert(ctx, &(ctx->thunks), 0);
    return 1;
}

void axolang_destroy(axolang_context_t *ctx) {
    pcc_context__destroy(ctx);
}
